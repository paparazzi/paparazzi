version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Run test
    task:
      jobs:
        - name: test
          commands:
            - docker/run.sh -it paparazziuav/pprz-ci make test
      prologue:
        commands:
          - docker pull paparazziuav/pprz-ci
          - checkout
          - git submodule update --init --force --recursive
    dependencies: []
